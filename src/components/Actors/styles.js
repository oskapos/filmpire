import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
  image: {
    borderRadius: '20px',
    boxShadow: '0.5em 0.5em 1em',
    maxWidth: '90%',
    objectFit: 'cover',
  },
}));
